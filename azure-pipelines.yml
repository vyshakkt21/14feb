# trigger:
# - master
trigger: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  serviceConnection: 'wif_tf_2026'
  resourceGroup: rg-vys-str
  storageAccount: backendstorage21
  container: tfstate
  tfstateFile: terraform.tfstate
  anyTfChanges: false

steps:

- task: TerraformInstaller@0
  displayName: install Terraform '1.6.6'
  inputs:
    terraformVersion: '1.6.6'

- task: TerraformTaskV1@0
  displayName: terraform init
  inputs:
    provider: 'azurerm'
    command: 'init'
    workingDirectory: '$(System.DefaultWorkingDirectory)/main/14feb/Linux_machine'
    backendServiceArm: 'wif_tf_2026'
    backendAzureRmResourceGroupName: 'rg-vys-str'
    backendAzureRmStorageAccountName: 'backendstorage21'
    backendAzureRmContainerName: 'tfstate'
    backendAzureRmKey: 'terraform.tfstate'
    
- task: TerraformTaskV1@0
  displayName: terraform plan -out=tfplan
  inputs:
    provider: 'azurerm'
    command: 'plan'
    workingDirectory: '$(System.DefaultWorkingDirectory)/main/14feb/Linux_machine'
    commandOptions: '-out=tfplan'
    environmentServiceNameAzureRM: 'wif_tf_2026'

- task: PowerShell@2
  displayName: detect any terraform change
  inputs:
    targetType: 'inline'
    script: |
      # Write-Host "LASTEXITCODE : $LASTEXITCODE"
      $plan = $(terraform show -json tfplan | ConvertFrom-Json)
      $actions = $plan.resource_changes.change.actions
      Write-Host "Terraform actions : $actions"
      if (($actions -contains 'create') -or ($actions -contains 'delete') -or ($actions -contains 'update'))
      {
        Write-Host "Terraform will perform the following actions : $actions"
        Write-Host "##vso[task.setvariable variable=anyTfChanges;]true"
      }
      else
      {
        Write-Host "There is no change detected in Terraform tfplan file"
      }
    workingDirectory: '$(System.DefaultWorkingDirectory)/main/14feb/Linux_machine'

- task: TerraformTaskV1@0
  displayName: terraform apply tfplan
  condition: eq(variables.anyTfChanges, true)
  inputs:
    provider: 'azurerm'
    command: 'apply'
    workingDirectory: '$(System.DefaultWorkingDirectory)/main/14feb/Linux_machine'
    commandOptions: 'tfplan'
    environmentServiceNameAzureRM: 'wif_tf_2026'